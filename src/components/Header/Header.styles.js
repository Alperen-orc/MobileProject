import { StyleSheet } from "react-native";

export default StyleSheet.create({
    header: {
        backgroundColor: '#eee',
        padding: 15,
        flexDirection: 'row',
        alignItems: 'center',
      },
    
      headerText: {
        color: '#000',
        fontSize: 20,
        marginTop: 5,
      },
})